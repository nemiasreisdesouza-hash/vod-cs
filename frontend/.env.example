# Empty = same-origin /api proxy (recommended for docker + previews).
# Set to http://localhost:8000 only when the frontend runs separately from the proxy.
NEXT_PUBLIC_API_URL=
API_INTERNAL_URL=http://127.0.0.1:8000
